## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | Beautiful icons
date-fns | Date formatting
react-hook-form | Form management
zod | Schema validation
@hookform/resolvers | Zod resolver for forms
recharts | For visualizing mess ratings or academic progress (optional but good for dashboard)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
- Using Replit Auth for authentication (already set up).
- Using Wouter for routing.
- Using TanStack Query for data fetching.
- API endpoints are defined in `@shared/routes`.
- Dark mode support via CSS variables.
- Sidebar layout for main navigation.
